---
import {getLanguages, getTranslatedContent} from "../../lib/directus";
import BaseLayout from "../layouts/BaseLayout.astro";

const global = await getTranslatedContent("global", "pt-BR")

console.log(await getLanguages())

---

{ global.title }
